# Kế hoạch & Tiến độ — AI Notifier

> File này theo dõi: kiến trúc, việc ĐÃ LÀM, việc CẦN LÀM. Cập nhật sau mỗi task.
> Cập nhật lần cuối: 2026-07-05 (nhập liệu GIỌNG NÓI ở màn tạo rule — web Web Speech API, mobile ghi âm → Edge Function transcribe/Gemini; 0022+0023 đã chạy)
> Chi tiết CÁCH HOẠT ĐỘNG + KHẢ NĂNG của từng chức năng (loại rule, ví dụ cụ thể...): xem **[TINH_NANG.md](TINH_NANG.md)** — cập nhật file đó mỗi khi xong 1 chức năng mới.

## Mục tiêu sản phẩm
Người dùng mô tả bằng ngôn ngữ tự nhiên → AI tạo **rule** → hệ thống **tự quét tin thật nhiều nguồn 24/7** → gửi **thông báo** đúng chủ đề/điều kiện, kèm link bài gốc.

## Kiến trúc hiện tại
```
App Expo (web + mobile)
   │  invoke
   ▼
Supabase Edge Functions ──► Gemini API (Google Search grounding)
   ├─ generate-rule : NL → Rule (hỏi lại khi thiếu, không bịa)
   └─ run-monitor   : tìm tin thật → lọc điều kiện → insert notifications → gửi push
   ▲
pg_cron (mỗi 15 phút) → run-monitor (quét nền, lọc rule tới hạn theo lịch)
```
- AI: **Gemini server-side** (key trong Supabase secret), KHÔNG còn Ollama local.
- Project ref: `idtibfiyfywcugdvlqal`. Deploy: `npx supabase functions deploy <fn> --project-ref idtibfiyfywcugdvlqal`.
- Quy ước hội thoại: câu mở đầu `!` = toàn quyền, không hỏi lại. Việc gì tự làm được thì tự làm.

---

## ✅ ĐÃ LÀM
- [x] Pipeline tin THẬT đa nguồn bằng Gemini + Google Search grounding.
- [x] Chuyển AI lên server (Edge Functions), bỏ Ollama; key giấu trong secret.
- [x] Chạy nền 24/7 bằng pg_cron (mỗi 15 phút).
- [x] Thông báo **theo điều kiện**: rule có `condition` chỉ báo khi bài thỏa.
- [x] Thông báo **chi tiết hơn**: `content` đầy đủ + mục `details` (phân tích sâu).
- [x] Mỗi lần quét 1 rule **chỉ 1 thông báo** (MAX_PER_RUN = 1).
- [x] **Tần suất linh hoạt**: lưu số phút, tối thiểu 30 phút; "Theo điều kiện" quét 15 phút (ẩn).
- [x] **Đặt giờ cụ thể** (`run_at`, giờ VN): vd "Hằng ngày lúc 08:00".
- [x] Tạo rule **chặt chẽ**: thiếu info thì hỏi, yêu cầu vô lý thì giải thích.
- [x] **Sắp xếp rule** theo thời gian tạo (cũ → mới) ở Home + Rules.
- [x] **Push notification** (code): đăng ký token, gửi Expo Push từ run-monitor, bảng push_tokens.
- [x] Deploy generate-rule + run-monitor; cron đã bật.
- [x] **Tối ưu quota Gemini**: cron là bộ lập lịch DUY NHẤT (lọc `isDue` theo `last_run_at`+tần suất); Home không quét trùng nữa (chỉ đọc tin); refresh thủ công throttle 5'; client retry lỗi tạm (không retry 429); server gặp 429/503 thì DỪNG sớm (giữ `last_run_at` để quét lại) + trần 8 rule/lần.
- [x] **Trigger "thay đổi"**: lưu `last_value` mỗi rule; truyền giá trị lần trước vào Gemini để chấm `changed`/`matches_condition`; rule "theo điều kiện" chỉ báo khi số liệu THỰC SỰ đổi so với lần trước (migration 0008).
- [x] **Tách nhiều rule từ 1 câu**: generate-rule trả về MẢNG `rules` (tách chủ đề độc lập, giữ chung nếu cùng 1 thứ, vẫn hỏi lại khi thiếu); UI hiện nhiều card ("Rule i/N", xóa bớt được) + tạo hàng loạt 1 lượt. Tương thích ngược shape cũ.
- [x] **Phân quyền run-monitor**: anon key là công khai nên không tin được. Cron gọi bằng service_role → admin (quét tất cả); app gọi kèm JWT → xác thực, lấy userId TỪ token (bỏ qua userId body), chỉ quét rule của mình; ruleId người khác → 403; anon thuần → 401. Không cần thêm secret / sửa SQL (cron đã dùng service_role).
- [x] **"Để êm" theo rule (mute push)**: nút bật/tắt trong chi tiết rule — rule VẪN chạy & VẪN tạo thông báo (xem trong app), chỉ KHÔNG đẩy push về máy. Server `run-monitor` bỏ qua `sendPush` khi `rule.muted`; thẻ rule hiện nhãn "Để êm" (migration 0010 thêm cột `muted`).
- [x] **Nhóm thông báo theo ngày** (Hôm nay / Hôm qua / dd/mm) ở tab Thông báo.
- [x] **Empty state có nút hành động**: Home + Notifications trống → nút tạo rule.
- [x] **AI Insight động**: thẻ ở Home lấy tóm tắt tin quan trọng/mới nhất THẬT, bấm mở đúng thông báo.
- [x] **Onboarding lần đầu**: 3 slide giới thiệu khi đăng nhập lần đầu trên thiết bị (cờ `@onboarded`).
- [x] **Badge tab Alerts**: đã có sẵn; cap hiển thị "99+".
- [x] **Giờ yên lặng (tùy chỉnh)**: trong Settings có nút bật/tắt + 2 thanh trượt chọn giờ bắt đầu/kết thúc (vắt qua nửa đêm OK). Trong khung này server `run-monitor` KHÔNG đẩy push (tin vẫn vào app). Lưu trên DB `user_settings` để server đọc (migration 0011); slider dùng `@react-native-community/slider`.

## ⏳ ĐANG CHỜ NGƯỜI DÙNG (tôi không tự làm được)
- [x] **Chạy SQL `0009_notification_related.sql`** (cột `related_notification_id`) — user xác nhận đã chạy.
- [x] **Chạy SQL `0010_rule_muted.sql`** (cột `muted`) — user xác nhận đã chạy.
- [x] **Chạy SQL `0011_user_settings.sql`** (bảng `user_settings`) — đã phải DROP + tạo lại bảng sạch (bảng cũ trùng tên có FK sai → 23503); "Giờ yên lặng" đã lưu OK.
- [ ] **Chạy SQL** gộp (`run_at` + `push_tokens`) trong Supabase SQL Editor — tôi không có mật khẩu DB.
- [ ] **Đổi key Gemini** — key cũ đã lộ trong ảnh chụp lúc setup (bảo mật).
- [ ] **EAS init + dev build** để nhận push thật trên điện thoại (cần tài khoản Expo + thiết bị; build cloud tính phí).
- [x] **Chạy SQL `0012_admin_logs.sql`** (bảng `usage_logs` + `cron_runs`) — user đã chạy; Quota + Lịch sử cron đã hiện số liệu.
- [x] **Chạy SQL `0013_cron_detail.sql`** (cột `detail` cho cron_runs) — user xác nhận đã chạy 2026-07-03.
- [x] **Chạy SQL `0015_rule_notify_mode.sql`** (cột `notify_mode`) — user xác nhận đã chạy 2026-07-03; chế độ "chỉ tin quan trọng" dùng được đầy đủ.
- [x] **Chạy SQL `0016_rule_reminder.sql`** (cột `source_type` + `remind_at`) — user xác nhận đã chạy 2026-07-02. Nhắc hẹn ("nhắc tôi X ngày 20/7") đã hoạt động đủ; đẩy lại web (EAS Hosting) + OTA (branch preview) để xác nhận bản mới nhất.
- [x] **Chạy SQL `0017_reminder_tick.sql`** (cron phụ 'reminder-tick' MỖI PHÚT, SQL-gated) — user xác nhận đã chạy 2026-07-03. **CẦN VERIFY key đúng** (lần trước dán nhầm ANON key → 401): tạo thử nhắc hẹn "nhắc tôi 3 phút nữa" xem có bắn đúng phút không; nghi ngờ thì soi `select status_code, content from net._http_response order by created desc limit 5` (mong 200, không phải 401).
- [x] **Chạy SQL `0018_url_watch.sql`** (cột `watch_url` + `watch_auth`) — user xác nhận đã chạy 2026-07-03; theo dõi trang web theo URL (TINH_NANG.md §3g) dùng được đầy đủ.
- [x] **Deploy web + OTA** cho bản URL-watch — xong 2026-07-03 sau khi user cho phép tiếp tục: web live https://ai-notifier-new.expo.app (200), OTA update group ea18bda9 (android+iOS, runtime 1.0.0, branch preview).
- [x] **Chạy SQL `0020_rule_health.sql`** (cột `rules.last_error` + watchdog báo cron chết) — user xác nhận đã chạy 2026-07-03. Sức khỏe rule + còi báo cron chết hoạt động đầy đủ.
- [x] **Chạy SQL `0021_notifications_user_id.sql`** (nhắc hẹn tự XÓA HẲN sau khi nhắc nhưng giữ thông báo; dọn luôn nhắc hẹn đã xong còn sót) — user xác nhận đã chạy 2026-07-03. MỌI migration đã chạy đủ.
- [x] **Chạy SQL `0022_scheduled_tick.sql`** (tick mỗi phút xử lý cả rule GHIM GIỜ) — user chạy 2026-07-04, cron.schedule trả job id 14. Còn kiểm chứng thực tế: rule ghim giờ nổ đúng phút.
- [x] **Chạy SQL `0023_url_hash.sql`** (cột `last_content_hash` — hash-gate rule URL) — user chạy 2026-07-04, Success.

## 🗺 KẾ HOẠCH: Các loại theo dõi tiếp theo (lập 2026-07-02)
> Ý tưởng xương sống: hiện MỌI rule đều đi qua Gemini + Google Search grounding — vừa tốn quota
> (1.500/ngày), vừa dễ tin bịa/URL sai/tin nhạt. Hướng đi: thêm cột `source_type` cho rule,
> run-monitor thành ROUTER — mỗi loại có nguồn dữ liệu CHUYÊN BIỆT (API thật, 0 quota grounding),
> Gemini search chỉ là fallback cho chủ đề tự do.

### Pha A — Nền tảng router ✅ XONG 2026-07-02
- [x] **KHÔNG cần migration** (đổi so với kế hoạch): phân loại bằng `detectSourceType(keyword)` — hàm thuần chạy LÚC QUÉT nên rule cũ tự hưởng, user khỏi chạy SQL. Cột `source_type` override để dành khi cần (UI chọn tay / loại rss·reminder sau).
- [x] run-monitor router: weather/crypto/fx → provider; lỗi → fallback 'search' (không chết rule). Quy tắc chống-cướp-rule: crypto/fx chỉ nhận khi keyword có ý "giá/tỷ giá" ("tin tức bitcoin" vẫn là rule tin tức).

### Pha B — Provider 0-quota cho dữ liệu SỐ ✅ XONG 2026-07-02
- [x] **Thời tiết: Open-Meteo** (free, no key): tách địa danh từ keyword (`extractWeatherLocation`) → geocoding → forecast 2 ngày (hiện tại + hôm nay + ngày mai, mã WMO → tiếng Việt). Bản tin dựng bằng TEMPLATE — 0 call AI. Tiêu đề kèm ngày dd/MM → unique mỗi ngày.
- [x] **Crypto: CoinGecko** (free): map coin phổ biến (BTC/ETH/BNB/SOL/XRP/DOGE/ADA/TON) → giá USD + quy đổi VND + %24h. `value` máy-đọc ("67123.45 USD") để so biến động chuẩn.
- [x] **Tỷ giá: open.er-api.com** (free, no key): USD/VND tham khảo, nêu chênh so lần trước. (Vàng SJC chưa có API sạch → vẫn đi search.)
- [x] **Điều kiện chấm trên SỐ THẬT**: rule có condition → flash-lite (KHÔNG grounding, không đụng quota 1.500) chấm matches trên giá trị API; chống lặp trigger = cooldown 6h + bỏ cooldown khi biến động ≥3%. Rule định kỳ thường mode "chỉ tin quan trọng" → chỉ gửi khi đổi ≥1%.
- [x] Soi bộ lọc (Trang test) hiện **nhãn nguồn** (🌤 Open-Meteo / 🪙 CoinGecko / 💱 er-api / 🔍 Gemini search) — soi rule provider giờ 0 quota. Đã verify 3 API thật (Thanh Hóa 27°C ✓, BTC $61.4k ✓, VND 26.251 ✓). 94 test pass (thêm 11: detect/extract/coin/WMO/compose/significantChange).

### Pha C — Tin tức qua RSS ✅ XONG 2026-07-02
- [x] `_shared/rss.ts`: parser RSS thuần (regex, edge runtime không có DOMParser) + feed map theo category (VnExpress chuyên mục + CafeF thị trường + Tuổi Trẻ; đã verify sống). `sourceFromLink` map hostname → tên báo.
- [x] run-monitor: đường RSS chạy TRƯỚC grounding cho mọi rule search: fetch feed song song → lọc bài CHƯA GỬI (dedup tiêu đề feed, ổn định tuyệt đối) → flash-lite CHỌN bài hợp chủ đề + tóm tắt (1 call, KHÔNG grounding) → link bài = link THẬT từ feed. Feed không có gì mới / không bài liên quan → filler theo luật chung, KHÔNG đốt grounding. Chỉ khi hạ tầng RSS/flash-lite hỏng mới rơi xuống Gemini + Google Search (grounding thành phương án cuối). Refactor: `sendFallback` dùng chung 2 đường.
- [x] Soi bộ lọc hiện nhãn "📰 RSS báo VN". → Quota grounding 1.500/ngày giờ gần như chỉ còn dùng khi RSS sập.

### Pha D — Loại rule mới ✅ Nhắc hẹn XONG 2026-07-02 (digest còn lại)
- [x] **Nhắc hẹn (reminder)**: migration `0016_rule_reminder.sql` (cột `source_type` + `remind_at` — CHỜ USER CHẠY). generate-rule nhận diện "nhắc tôi X ngày 20/7 lúc 9h" (prompt được tiêm ngày giờ VN hiện tại để quy đổi; thiếu ngày thì hỏi lại; lưu ISO +07:00). isDue: tới hạn khi QUA remind_at & chưa quét sau mốc (nhắc muộn vẫn nhắc). run-monitor: bắn push "⏰ Nhắc hẹn" (0 call AI, is_important, nêu độ trễ nếu muộn >15') rồi TỰ TẮT rule. Client: card preview hiện "Nhắc lúc"; insert chỉ đính kèm 2 cột khi là reminder (rule thường không đụng cột mới → chưa chạy 0016 vẫn tạo rule thường bình thường); banner lọc rác bỏ qua reminder.
- [ ] **Bản tin gộp buổi sáng (digest)**: 1 push 7h30 gộp thời tiết + giá + tin của mọi rule trong ngày; từng rule đánh dấu "gộp vào digest" thay vì push lẻ. *(phần cuối còn lại của Pha D — cần chốt UX: giờ digest per-user hay per-rule)*

### Pha E — Theo dõi trang web cụ thể ✅ XONG 2026-07-03 (chi tiết: TINH_NANG.md §3g)
- [x] **Theo dõi URL cụ thể** (`source_type='url'`, migration 0018): fetch trang (chống SSRF, UA thật,
      timeout 15s, cap 400KB) → stripHtml → flash-lite trích value/tóm tắt (0 grounding) → cổng gửi
      giống provider (condition/đặt giờ/important-mode so đổi). Lỗi trang → bỏ lượt, KHÔNG rơi xuống search.
- [x] **Trang cần đăng nhập — cấp quyền**: 401/403 hoặc AI thấy nội dung che sau login → thông báo 🔒
      hướng dẫn (1 lần, không spam); chi tiết rule có mục "Cấp quyền đăng nhập" dán Cookie / `Header: value`
      (cột `watch_auth`, RLS bảo vệ); hết hạn → báo cấp lại; "Thu hồi quyền" xóa được.
- [ ] Giá sản phẩm Shopee/Tiki — chặn bot + ToS; rule url thử được nhưng trang render JS thuần (SPA)
      fetch không thấy nội dung (cần headless browser — để sau).

### Vì sao thứ tự này
- B trước C: rule của user hiện tại đa số là SỐ LIỆU (thời tiết, vàng, tỷ giá, BTC) — B giải đúng đau hiện tại (bản tin 8h + trigger điều kiện), lại dễ (API public, không key hoặc key free).
- C giải nốt quota + link sai cho rule tin tức tự do.
- D là tính năng mới hẳn (reminder) + UX (digest), làm khi nền đã vững.

## 📋 CẦN LÀM TIẾP (backlog, ưu tiên trên xuống)
- [x] **Polish UI/UX**: tìm rule (header search), hiện lịch + điều kiện trên thẻ rule (Rules + Home), trạng thái rỗng theo ngữ cảnh; notifications đã có sẵn search/filter/empty/swipe-delete.
- [x] **Lọc thông báo theo rule**: chips ngang dưới TABS (chỉ hiện khi >1 rule); bấm chip chọn/bỏ chọn; kết hợp được với tab Chưa đọc/Quan trọng và search.
- [x] **Chia sẻ thông báo**: nút share-social trên header notification-detail → native Share sheet; nội dung = tiêu đề + tóm tắt AI (hoặc nội dung) + link bài gốc.
- [ ] badge unread trên tab; ẩn nút "Đọc bài gốc" đã xong (chuyển thành "Xem thông báo trước").
- [x] **Trang quản trị (Admin Dashboard) — CHỈ ADMIN** — Pha 1/2/3 XONG (xem kế hoạch bên dưới). Còn chờ user chạy SQL 0012 để bật Quota/Cron.

---

## 🛠 KẾ HOẠCH: Trang quản trị (Admin Dashboard) — chỉ chủ app
> Mục tiêu: 1 nơi để theo dõi & vận hành toàn hệ thống (mọi user/rule/cron/quota), không phải mò Supabase Dashboard.

### Nơi đặt (đề xuất)
- **Route `/admin` trong chính app Expo hiện tại** (web là chính), KHÔNG dựng project web thứ 2.
- Lý do: tái dùng auth + Supabase client + pipeline deploy/OTA sẵn có; chỉ thêm 1 màn ẩn, vào bằng tài khoản admin. Trên mobile ẩn entry, dùng chủ yếu trên trình duyệt.

### Bảo mật (BẮT BUỘC — không được phá RLS)
- RLS hiện chặn mỗi user chỉ thấy dữ liệu của mình. Admin cần đọc TẤT CẢ → KHÔNG nới RLS.
- Cách đúng: **Edge Function `admin-api` (service_role)** xác thực JWT → kiểm tra user nằm trong **allowlist admin** → mới trả dữ liệu tổng hợp. Client `/admin` chỉ gọi function này.
- Cần: bảng `admins(user_id)` (hoặc allowlist env) để phân quyền; gate UI + gate server (2 lớp).

### Tính năng (chia pha)
- **Pha 1 — MVP (xem + vận hành cơ bản)**
  - Tổng quan: số user, số rule (bật/tắt/muted), số thông báo (hôm nay/tổng), token push.
  - Danh sách RULE toàn hệ thống: chủ đề, tần suất, `last_run_at`, `muted`, `last_value`; lọc/tìm.
  - Nút **chạy thử 1 rule** (admin trigger run-monitor) để soi lỗi.
- **Pha 2 — Người dùng & nội dung**
  - Danh sách user: email, ngày tạo, số rule, số thông báo, hoạt động cuối; mở chi tiết 1 user.
  - Stream thông báo mới nhất toàn hệ thống (soi chất lượng AI + link đúng/sai), lọc category/sentiment.
- **Pha 3 — Vận hành & quota**
  - Lịch sử cron (thành công/lỗi/HTTP status). *(cần hạ tầng log — xem dưới)*
  - Theo dõi **quota Gemini/ngày** + cảnh báo gần ngưỡng 1.500. *(cần log usage)*
  - Gửi **push test**; quản lý danh sách admin.

### Hạ tầng còn THIẾU phải làm trước (gaps)
1. `admins` (phân quyền) + Edge Function `admin-api` (service_role, check admin).
2. **Log quota Gemini**: hiện KHÔNG đếm số call/429 → thêm bảng `usage_logs` (hoặc đếm trong run-monitor) thì Pha 3 mới có số liệu.
3. **Log cron run**: tạo bảng `cron_runs` (mỗi lần run-monitor: số rule quét, lỗi, thời lượng) — hoặc đọc tạm `net._http_response`.

### Thứ tự làm gợi ý
admins + admin-api (service_role) → route `/admin` gate → Pha 1 (overview + rule list + chạy thử) → log usage/cron → Pha 2 → Pha 3.

### Tiến độ
- [x] **Pha 1 (2026-06-20)**: Edge Function `admin-api` (service_role + gate admin theo secret `ADMIN_EMAILS`, hiện = tonghiep1607@gmail.com); `lib/admin.ts`; màn `app/admin.tsx` (overview + danh sách mọi rule + nút "Chạy thử"); lối vào trong Settings (chỉ admin); route đăng ký ở `_layout.tsx`. Smoke test gọi không auth → 401. Đổi danh sách admin = `npx supabase secrets set ADMIN_EMAILS="a@x,b@y" --project-ref idtibfiyfywcugdvlqal` (KHÔNG cần đổi code).
- [x] **Pha 2 (2026-06-20)**: admin-api actions `users`/`user_detail`/`notifications`; màn `app/admin-users.tsx` (danh sách + thống kê), `app/admin-user-detail.tsx` (info + rule + tb + token + giờ yên lặng + nút push test), `app/admin-notifications.tsx` (stream toàn hệ thống + lọc sentiment). Nút điều hướng Người dùng/Thông báo trên trang admin.
- [x] **User UX (2026-06-20)**: (1) Đã-đọc/chưa-đọc: kiểm tra thấy ĐÃ CÓ SẴN đầy đủ (badge tab, đánh dấu đã đọc khi mở chi tiết, nút "Đọc hết", tab "Chưa đọc", viền+chấm). (2) Mẫu rule TẠO NGAY: `QUICK_TEMPLATES` ở create-rule (Giá vàng/Tỷ giá USD/Bitcoin/Xăng/Thời tiết HN/Tin CN) — insert thẳng, KHÔNG gọi AI nên dùng được cả khi kẹt quota. (3) Giảm thông báo rác: run-monitor chỉ gửi filler "chưa có thay đổi/chưa tìm thấy" ≤ 1 lần/ngày (bỏ qua nếu rule đã có tb trong ngày VN); tin THẬT vẫn gửi bình thường. *Digest gom-nhiều-rule-thành-1 chưa làm (lớn hơn).*
- [x] **Pha 4 — Quản lý (2026-06-20)**: admin-api action `rule_action` (toggle_active/toggle_muted/delete) + `delete_user` (cascade). admin.tsx: chip lọc rule theo trạng thái (Tất cả/Đang bật/Tắt/Để êm) + hàng nút Tạm dừng·Để êm·Xóa trên mỗi thẻ rule. admin-user-detail.tsx: nút "Xóa người dùng" (confirm, cascade rule/tb/token; chặn tự xóa admin đang dùng).
- [x] **Pha 3 (2026-06-20)**: migration `0012_admin_logs.sql` (usage_logs + cron_runs, RLS no-policy → chỉ service_role). run-monitor ghi `usage_logs` mỗi call Gemini + `cron_runs` mỗi lần chạy (best-effort, bảng chưa có thì nuốt lỗi). admin-api actions `usage` (quota 7 ngày, ngưỡng 1.500), `cron_runs` (40 lần gần nhất), `push_test`. Trang admin hiện thanh Quota + lịch sử cron; chờ user chạy SQL 0012 để có số liệu.

---

## 🐞 Lỗi đã gặp & cách giải quyết
Giải thích chi tiết từng lỗi (hệ thống, ảnh hưởng mọi rule) ở **[PROJECT_CONTEXT.md §3](PROJECT_CONTEXT.md#3--lỗi-đã-gặp--cách-giải-quyết)**. Tóm tắt:
1. **Dedup khóa cứng theo URL** → chủ đề số liệu (thời tiết/giá) bị chặn vĩnh viễn → dedup theo URL + `last_value`.
2. **Bỏ đói rule khi >8 rule** → sắp theo `last_run_at` tăng dần, xoay vòng công bằng.
3. **Đốt quota Gemini** → cron là lịch duy nhất, bỏ auto-scan client, dừng sớm khi 429, trần 8 rule/lần.
4. **Quét xong im lặng** → rule định kỳ/đặt giờ LUÔN gửi (tin / "chưa thay đổi" + link cũ / "chưa tìm thấy" + tin liên quan).
5. **Rule ghim giờ không đúng giờ** → khung [giờ hẹn, +15'), guard chu kỳ, xử lý quanh nửa đêm.
6. **Rule mơ hồ** → xử lý lúc tạo: hỏi lại, không bịa điều kiện.
7. **Anon key công khai lạm dụng run-monitor** → xác thực JWT, lấy userId từ token, chặn 401/403.

---

## Nhật ký thay đổi
- 2026-07-06 (fix nóng 3): **VẪN SẬP trên binary cũ dù đã lazy-require + try/catch** — user gửi crash dialog: `Cannot find native module 'ExpoAudio'`, stack có `guardedLoadModule → getAudioMod`. Nguyên nhân sâu: với LAZY require, metro `guardedLoadModule` KHÔNG ném lỗi factory lại cho try/catch bên ngoài mà gọi thẳng `ErrorUtils.reportFatalError` → sập app, try/catch vô dụng. Fix đúng: DÒ TRƯỚC bằng `requireOptionalNativeModule('ExpoAudio')` (expo-modules-core — trả null, không throw) rồi mới require; áp cả `ExponentFileSystem`. Jest: mock partial expo-modules-core (requireOptionalNativeModule → {}). tsc + 140 test pass. OTA a86bdd7f. BÀI HỌC CHUẨN: feature-detect native module = requireOptionalNativeModule, KHÔNG BAO GIỜ try/catch quanh require.
- 2026-07-05 (fix nóng 2): **MÀN TRẮNG tạo rule trên ĐIỆN THOẠI (binary cũ)** — user gửi ảnh màn trắng xóa khi vào tạo rule trên app cài máy. Nguyên nhân: app trên máy là BUILD CŨ (trước khi thêm expo-audio) nhận OTA JS mới; import tĩnh "expo-audio" gọi `requireNativeModule('ExpoAudio')` NGAY LÚC LOAD file — binary không có native module → throw → màn trắng (bản OTA release không có màn lỗi đỏ). Fix `lib/voiceInput.ts`: (1) require LƯỜI expo-audio + expo-file-system/legacy trong try/catch — thiếu native module → `supported=false`, nút mic tự ẩn, màn chạy bình thường; (2) bỏ hook `useAudioRecorder` (hook buộc import tĩnh) — tự `new AudioModule.AudioRecorder(options)` (tự map options như createRecordingOptions nội bộ vì hàm đó không export) + `release()` khi rời màn. Cập nhật mock jest theo shape mới. tsc + 140 test + lint sạch. OTA 0883ed75. HỆ QUẢ: muốn DÙNG mic trên app cài máy phải build APK/binary MỚI (OTA không thêm được native module — chỉ Expo Go/binary mới có mic; binary cũ chỉ hết sập + ẩn nút). BÀI HỌC: thêm dependency native mới = OTA cũ sập nếu import tĩnh → luôn lazy-require + feature-detect khi còn binary cũ ngoài đồng.
- 2026-07-05 (fix nóng): **WEB SẬP MÀN TẠO RULE sau khi thêm giọng nói** (user báo "lỗi nội bộ, không vô được phần tạo rule"). Nguyên nhân: import `expo-audio` trên web — `AudioRecorderWeb extends globalThis.expo.SharedObject` chạy NGAY LÚC IMPORT module, môi trường web export tĩnh không có runtime đó → crash cả màn. Fix: tách hook theo PLATFORM EXTENSION — `lib/voiceInput.web.ts` (chỉ Web Speech API, 0 import expo-audio/file-system) + `lib/voiceInput.ts` (bản mobile như cũ) + `lib/voiceInput.types.ts` (interface chung); Metro tự chọn file .web cho web. Verify: grep bundle dist không còn "AudioRecorderWeb", tsc + 140 test pass, web deploy lại 200, OTA ba518eca. BÀI HỌC: thư viện expo có native module → LUÔN tách .web.ts, đừng dựa vào check Platform.OS trong cùng file (import vẫn chạy).
- 2026-07-05: **NHẬP LIỆU GIỌNG NÓI ở màn tạo rule** (TINH_NANG §2e). Nút 🎙 cạnh ô chat, chữ đổ vào ô nhập để xem lại rồi mới gửi. 2 đường: (a) WEB = Web Speech API trình duyệt (vi-VN, interim results — chữ hiện dần, 0 quota; Firefox không có → nút tự ẩn); (b) MOBILE = expo-audio ghi âm m4a/AAC (tự dừng 60s) → base64 → Edge Function mới `transcribe` (bắt buộc JWT đăng nhập, trần 4MB base64, đếm usage_logs) → Gemini flash-lite chép lời (gemini.ts thêm hỗ trợ `audio` inline_data). Files: `lib/voiceInput.ts` (hook useVoiceInput, 2 đường tự chọn theo platform), `supabase/functions/transcribe/index.ts`, nút mic + placeholder động ở `app/create-rule.tsx`, app.json plugin expo-audio (microphonePermission), mock expo-audio/expo-file-system trong jest setup. +1 test (140 pass, 11 suite), tsc + eslint sạch. Deploy: transcribe + web (LƯU Ý VẬN HÀNH: `eas update` GHI ĐÈ dist/ bằng bundle native — phải `expo export --platform web` LẠI trước khi `eas deploy`, không thì web 404) + OTA update b79315d5 (branch preview). Chưa kiểm tay trên điện thoại thật (Expo Go) — cần user thử.
- 2026-07-04 (đợt 2): **NÂNG CẤP HỆ QUÉT — 4 mục cốt lõi** (bàn phương án, user chốt phạm vi "chỉ 4 mục"). (1) **Tick mỗi phút cho rule GHIM GIỜ**: mở rộng cron reminder-tick (0017) thành tick chung — body `{"tick":true}` (giữ `reminderOnly` làm alias), run-monitor lọc bằng `isTickDue` (nhắc hẹn tới hạn + rule ghim giờ trong cửa sổ [mốc, +10')); bản tin 8:00 nổ đúng 8:00±1' thay vì lệch theo nhịp cron 15'. Chống bắn đúp khi tick chạy chồng cron chính: CLAIM nguyên tử (UPDATE có điều kiện trên `last_run_at`, giống monitorReminder) áp cho rule ghim giờ ở CẢ 2 đường; lỗi quota sau claim → trả lại `last_run_at` cũ để khung catch-up 4h còn cứu. Trigger cron_runs mới: "tick". → CHỜ USER chạy `0022_scheduled_tick.sql` (thay key như 0019). (2) **Hash-gate rule URL**: `contentFingerprint` (FNV-1a trên text trang đã gọn khoảng trắng) lưu vào `rules.last_content_hash`; lượt sau trang Y NGUYÊN → skip ngay 0 call AI (không áp cho quét manual + rule ghim giờ). Cột chưa có → tự ghi lại không kèm (pattern last_error). → CHỜ USER chạy `0023_url_hash.sql`. (3) **Cache RSS theo category trong 1 lượt run** (`RssCache` Map, cache cả null) — nhiều rule cùng category chỉ fetch bộ feed 1 lần. (4) **Dedup 2 lớp tiêu đề + LINK** (`normLink`: bỏ utm_*/fbclid/fragment/www/trailing-slash, sort query) cho đường RSS/feed — báo sửa tít nhẹ cùng link không lọt lại; đường grounding giữ dedup title (link Gemini không ổn định). +12 test (139 pass, 11 suite). Deploy run-monitor. Kiểm chứng sau khi chạy 0022: tạo rule ghim giờ ở phút kế tiếp → nổ đúng phút (cron_runs trigger='tick'); để tick+cron chính cùng qua 1 mốc → đúng 1 thông báo.
- 2026-07-04: **FIX "thời tiết đặt 8h nhưng báo lúc 10h"** (user xác nhận rule có run_at=08:00, tin đến 10h = bắn trong khung vớt 4h vì bị chèn hàng 2 tiếng). 2 lỗi gốc ở hàng đợi quét: (1) `dueAt` của rule GHIM GIỜ tính last_run_at+chu kỳ — hôm trước bắn muộn (sự cố cron chết) thì 8h sáng hôm sau hạn bị tính thành ~10h → rule xếp CUỐI hàng đợi, sau cả đống rule tin tức tồn đọng; sửa: trong khung bắn, dueAt = MỐC HẸN hôm nay (08:00). (2) Gặp lỗi quota giữa lượt là `break` cả lượt → rule 0-AI (thời tiết đi Open-Meteo) chết chùm vì đứng sau rule AI hết quota; sửa: thêm `scanTier` (nhắc hẹn → ghim giờ → định kỳ trơn) sort trước dueAt, và quota lỗi chỉ `continue` — các rule 0-AI (`isAiFreeRule`: reminder + provider không điều kiện) vẫn được quét nốt, rule dính quota giữ nguyên last_run_at để lượt sau thử lại. +2 test (127 pass). Deploy run-monitor. Kiểm chứng: bản tin 8h sáng 2026-07-05 phải đến 8h00-8h02.
- 2026-07-03 (tiếp 12): **Phân biệt 2 loại 429 của Gemini** (user thấy "quota quá tải" khi mới dùng 20/1500 lượt). Chẩn đoán từ ảnh admin: lỗi là `limit: 20 ... retry in 18s` = trần TỐC ĐỘ 20 lượt/PHÚT của flash-lite free (nghẽn tức thời vài giây), KHÔNG phải hết quota ngày (1500 grounding vẫn nguyên). Fix 3 lớp: (1) `_shared/gemini.ts`: 429 kèm "retry in Xs" (X≤30) → TỰ CHỜ X+1 giây rồi thử lại 1 lần — mọi caller (run-monitor, generate-rule) hấp thụ nghẽn phút mà không thấy lỗi; (2) generate-rule: vẫn lọt thì trả lời "⏳ AI bận tức thời, chờ ~X giây" thay vì câu hết-lượt-ngày gây hiểu lầm; (3) trang admin: ghi chú dưới thẻ quota giải thích trần ngày vs trần phút. Deploy run-monitor + generate-rule + web + OTA (update ea4d8967).
- 2026-07-03 (tiếp 11): **Tăng tốc loading toàn app** (user than: mở app load lại mọi thứ lâu, chuyển màn cũng trễ). 4 nguyên nhân + 4 fix: (1) **KHÔNG có cache** — mọi màn chờ mạng xong mới vẽ → thêm `lib/screenCache.ts` (AsyncStorage, kiểu "hiện ngay, làm mới ngầm"), áp cho Home + tab Thông báo + tab Rules: lần vào đầu vẽ NGAY dữ liệu lần trước, mạng về sau thay bản mới; (2) **truy vấn nối đuôi** → Home/tab Thông báo chạy 2 query SONG SONG (Promise.all), rule-detail cũng song song 2 query; (3) **tải toàn bộ lịch sử thông báo** (cả content dài) chỉ để đếm/hiện 1-2 thẻ → notifQuery thêm opts `{limit, unreadOnly, importantOnly}` — Home + tab Thông báo chỉ lấy 100 dòng gần nhất (search/lọc áp trên 100 dòng đó); (4) **query rules thừa** ở badge tab + profile → helper notifQuery tự fallback (tự lấy rule ids khi 0021 chưa chạy), bỏ 2 query phụ. Lưu ý còn lại: web cold-start bundle 3.2MB (output single, chưa code-split được) — chưa xử. tsc+eslint sạch, 125 test pass, deploy web + OTA (update 6000fcf3).
- 2026-07-03 (tiếp 10): **Nhắc hẹn TỰ XÓA HẲN sau khi nhắc, GIỮ thông báo (user chọn mức 2)**. Vấn đề gốc: notifications gắn rule bằng FK cascade + RLS chỉ cho xem qua rule → xóa rule là mất luôn thông báo nhắc. Sửa: (1) migration `0021_notifications_user_id.sql` (CHỜ USER CHẠY, không cần key): cột `notifications.user_id` (chủ sở hữu trực tiếp) + backfill + rule_id cho phép null + 4 policy RLS thêm nhánh `user_id = auth.uid()` + DỌN SÓT (detach + xóa nhắc hẹn đã xong còn lại từ trước); (2) run-monitor: insertNotif ghi kèm user_id (fallback bỏ cột nếu 0021 chưa chạy — không bao giờ mất thông báo); monitorReminder sau khi bắn: update notifications set user_id, rule_id=null → delete rule; update lỗi (0021 chưa chạy) → hành vi cũ (tắt rule); (3) client: `lib/notifQuery.ts` (fetch/count theo user_id, lỗi → fallback rule_id) áp vào 4 màn (notifications, badge tab _layout, Home, Profile) — thông báo mồ côi rule vẫn hiện đủ; types Notification.rule_id nullable + user_id; chip tên rule guard null; 2 test cũ khẳng định "không được lọc user_id trên notifications" viết lại theo kiến trúc mới. Mục "Nhắc hẹn đã xong" ở tab Rules GIỮ làm fallback khi 0021 chưa chạy. tsc sạch, 125 test pass. Deploy run-monitor + web + OTA (update a0bb9dd7). VIỆC USER: chạy `0021_notifications_user_id.sql`.
- 2026-07-03 (tiếp 9): **Nhắc hẹn đã xong hết "lởn vởn" trong danh sách rule** (user báo: nhắc xong mất ở Home nhưng vẫn nằm ở tab Rules như quên chưa xóa). KHÔNG tự xóa hộ (xóa rule kéo thông báo nhắc bị xóa theo — FK cascade 0014), thay vào đó: `isDoneReminder` (reminder + is_active=false + last_run_at ≥ remind_at) tách khỏi danh sách chính (kể cả tab Tạm dừng) → gom vào mục gấp gọn **"Nhắc hẹn đã xong (n)"** cuối trang: từng dòng có "Đã nhắc lúc HH:MM dd/mm" + nút xóa, ≥2 cái có "Xóa tất cả" (confirm nói rõ xóa kèm thông báo). Chi tiết rule hiện trạng thái "Đã nhắc xong" thay vì "Tạm dừng" khó hiểu. User xác nhận ĐÃ CHẠY 0020. tsc sạch, 125 test pass, deploy web + OTA (update d06e4ce1).
- 2026-07-03 (tiếp 8): **3 nâng cấp user chốt ("làm 145 đi")**. (1) **SỨC KHỎE HỆ THỐNG** — trị đúng cái đau "chết im không ai biết": (a) migration `0020_rule_health.sql` (CHỜ USER CHẠY, không cần thay key): cột `rules.last_error` + bảng `watchdog_alerts` + cron `watchdog` THUẦN SQL mỗi 15' — quét nền ngừng >50' là push thẳng cho admin qua Expo Push API (không qua Edge Function nên không chết chùm; chống spam 1 lần/6h); (b) run-monitor ghi `last_error` mỗi lượt (lỗi url trước đây bị nuốt im ở router giờ ném lên vòng ngoài để ghi), quét êm thì xóa; cột chưa có thì tự ghi lại không kèm (không mất last_run_at); (c) rule-detail hiện "Quét lần cuối: X phút trước" + hộp ⚠️ lỗi lần quét gần nhất. (2) **SỬA RULE BẰNG CHAT**: generate-rule nhận `edit_rule` (rule hiện tại) → prompt chế độ sửa (chỉ đổi phần được yêu cầu, giữ nguyên còn lại, luôn 1 rule); lib/ruleAI thêm `editRuleAI` (refactor invokeGenerateRule dùng chung retry); rule-detail có card "Sửa nhanh bằng chat" → tóm tắt thay đổi từng trường ("• Lịch: Hằng ngày 08:00 → 07:00") → confirmAsync (thêm tham số nhãn nút, mặc định giữ "Xóa") → update đúng cột đổi. (3) **ĐĂNG NHẬP 1-CHẠM (mobile)**: cài react-native-webview (có sẵn trong Expo Go); màn `grant-login` mở trang trong WebView, inject `document.cookie` sau mỗi bước điều hướng, đủ cookie → nút "Cấp quyền" lưu watch_auth rồi replace về rule-detail; web fallback hướng dẫn thủ công (browser chặn cookie chéo nguồn); giới hạn HttpOnly ghi rõ trong app + TINH_NANG. tsc+eslint sạch, 125 test pass. Deploy run-monitor + generate-rule + web + OTA (update 931176c3). VIỆC USER: chạy `0020_rule_health.sql` trong SQL Editor.
- 2026-07-03 (tiếp 7): **Trang danh sách phải GIẢI THÍCH từng mục** (user: trending GitHub mới chỉ liệt kê tên dự án, chưa nói dự án đó để làm gì). Siết QUY TẮC TRANG DANH SÁCH trong prompt extractUrlAI: mỗi mục BẮT BUỘC 1 câu tiếng Việt "nó là gì / dùng để làm gì" DỊCH từ mô tả trên trang (không bịa; trang không ghi thì chú "(trang không mô tả)") + số liệu; `details` không lặp danh sách mà rút XU HƯỚNG chung ("4/7 dự án hôm nay về AI agent..."). Nâng text trang đưa AI 10k→12k ký tự (bằng cap stripHtml) để mô tả các mục cuối không bị cắt. Deploy run-monitor. Kiểm chứng: đợi bản tin trending sáng mai hoặc bấm "Kiểm tra tin ngay" trên rule.
- 2026-07-03 (tiếp 6): **Bộ KỊCH BẢN kiểm thử hệ thống** (user bỏ digest, muốn soi cách hệ thống xử lý đa dạng yêu cầu). `scripts/scenario-test.mjs` — 23 kịch bản 7 nhóm gọi generate-rule THẬT; kết quả ghi ở **KICH_BAN_TEST.md**. Đợt 1: 10/10 kịch bản chạy được đều ĐÚNG (provider/tách rule/hỏi lại/từ chối MXH kèm gợi ý/bản đồ nguồn GitHub); 13 kịch bản kẹt vì flash-lite 503/429. PHÁT HIỆN + VÁ: chat trả nguyên JSON lỗi Gemini khi hết lượt → generate-rule giờ trả "⏳ AI đang quá tải..." thân thiện (deploy). Ghi nhận rủi ro mới: flash-lite thành điểm nghẽn quota (pick+enrich+extract+condition) — theo dõi usage_logs rồi tính. Việc treo: chạy lại 13 kịch bản sau reset quota (~14h VN); đợt 2 test kịch bản QUÉT cần tài khoản test.
- 2026-07-03 (tiếp 5): **CHẨN ĐOÁN "thời tiết im từ hôm qua" + sửa chất lượng trending + hết hỏi quyền oan**. (1) NGUYÊN NHÂN NGHI SỐ 1 vụ mất thông báo nền: user "chạy lại mọi migration" → 0004 chạy với placeholder `<SERVICE_ROLE_KEY>` nguyên văn + 0017 trong repo bị COMMIT NHẦM ANON KEY ("role":"anon") → cả 2 cron gọi run-monitor 401 → quét nền chết im (chỉ "Kiểm tra tin ngay" thủ công còn chạy). Fix: `0019_fix_cron_keys.sql` lập lại CẢ 2 job (thay `<SERVICE_ROLE_KEY>` 1 loạt bằng legacy service_role JWT — CHỜ USER CHẠY); 0017 đã gỡ anon key khỏi repo. (2) Trending chung chung: prompt extractUrlAI thêm QUY TẮC TRANG DANH SÁCH — liệt kê đích danh 5-8 mục "• Tên — mô tả (số liệu)", title nêu tên dẫn đầu. (3) Hỏi quyền oan trên trang công khai: định nghĩa login_required siết lại (nút Sign in trên menu ≠ bắt đăng nhập) + chốt chặn code: text trang >6000 ký tự → không bao giờ gửi thông báo 🔒. Deploy run-monitor. 125 test pass.
- 2026-07-03 (tiếp 4): **Sửa 2 phàn nàn của user**. (1) **Thông báo sơ sài** — nguyên nhân: đường RSS/feed chỉ tóm 1-2 câu từ MÔ TẢ FEED (rất ngắn), mục "Phân tích chi tiết" bỏ trống. Sửa: `enrichFromArticle` — sau khi chọn bài, fetch BÀI GỐC (fetchWatchPage, best-effort) → flash-lite viết content 4-6 câu + details phân tích + ai_summary từ TEXT THẬT của bài; áp cho cả đường RSS tin tức (Pha C) lẫn feed của rule url; enrich lỗi thì giữ bản tóm cũ, không hỏng thông báo. extractUrlAI (đọc trang) cũng trả thêm `details`. (2) **Flow cấp quyền khó dùng** — giờ: thông báo 🔒 có NÚT "Cấp quyền truy cập" đi thẳng tới rule-detail (param grant=1 → viền nổi khối cấp quyền); khối cấp quyền mới có nút "Mở trang để đăng nhập" (Linking) + "Cho phép" (lưu cookie) + "KHÔNG cho phép" (tạm dừng rule, thôi nhắc) / đã cấp thì "Thu hồi quyền"+"Cập nhật"; lời hướng dẫn viết lại bớt kỹ thuật. 125 test pass, tsc+eslint sạch. Deploy run-monitor + web (https://ai-notifier-new.expo.app) + OTA (update fda7c555).
- 2026-07-03 (tiếp 3): **Pha E3 — app TỰ HIỂU yêu cầu, người dùng khỏi biết link kỹ thuật**. Khảo sát MXH bằng fetch thật: ✅ Telegram (t.me/s/), YouTube (feed), Reddit (.rss), Bluesky (/rss — trang cũng tự khai feed), Mastodon (.rss), Substack, GitHub (trending render server + releases.atom); ⚠️ X chỉ thấy bio (đọc tweet phải API trả phí — từ 2/2026 X bỏ hết gói free đọc, pay-per-use $0.005/post); ❌ FB/IG/Threads/TikTok (vỏ rỗng/JSON máy). Làm 3 việc: (1) `normalizeWatchUrl` (thuần + test): t.me/kênh→/s/, reddit /r/x→.rss, bsky profile→/rss, youtube /channel/UC…→feeds/videos.xml — dán link quen tay nào cũng chạy; áp trong `ruleWatchUrl`. (2) `parseRss` hiểu **Atom** (<entry>, link ở attr href, summary/content, published/updated — Reddit/YouTube/GitHub đều Atom); `looksLikeFeed` + nhánh watch_url-là-feed-trực-tiếp trong monitorUrl (refactor `monitorFeedItems` dùng chung với đường feed khám phá). (3) generate-rule: mục "BẢN ĐỒ NGUỒN PHỔ BIẾN" — "dự án nổi bật GitHub mỗi sáng" tự ra github.com/trending (VD L), đòi FB/IG/TikTok/X → giải thích + gợi ý thay thế (VD M). 125 test pass (thêm 7), tsc+eslint sạch. Deploy run-monitor + generate-rule (smoke 401 OK) — chỉ server, không cần web/OTA.
- 2026-07-03 (tiếp): **Hoàn thiện rule URL cho TRANG TIN TỨC (Pha E2)** — user chốt bỏ Facebook (đã test: FB trả vỏ rỗng 1.5KB cho bot, JS render + chống bot, cookie không cứu; Telegram t.me/s/... và YouTube RSS thì ăn ngay). 3 nâng cấp `monitorUrl`: (1) **Ưu tiên feed trang tự khai báo** — `discoverFeedUrl` bắt `<link rel=alternate type=rss+xml>` (Tuổi Trẻ/CafeF/WordPress có; đã verify), đọc feed → bài chưa gửi → `pickRssItemAI` chọn + tóm tắt → link bài THẬT, dedup chuẩn; không liên quan/không mới → rơi xuống đọc trang (không cướp rule giá). (2) **Đọc trang kèm chọn link bài**: `extractPageLinks` bóc anchor dài (tiêu đề bài) từ HTML thô, AI trả `link_index` → thông báo trỏ đúng BÀI thay vì trang chủ (VnExpress không khai feed vẫn có link đúng). (3) **Chống trùng**: so tiêu đề đã gửi (60 gần nhất) + giá trị — nội dung y nguyên thì im (trừ rule đặt giờ), hết cảnh spam lại cùng 1 bài mỗi chu kỳ. webwatch trả thêm `html` thô; previewGate soi kèm link. 118 test pass (thêm 5: discoverFeedUrl/extractPageLinks), tsc+eslint sạch. Deploy run-monitor (smoke 401 OK) — chỉ đổi server, KHÔNG cần deploy web/OTA.
- 2026-07-03: **Pha E — Theo dõi TRANG WEB/APP cụ thể theo URL (kể cả trang cần đăng nhập)**. (1) Migration `0018_url_watch.sql` (CHỜ USER CHẠY): cột `watch_url` + `watch_auth`. (2) `_shared/webwatch.ts` fetch trang: UA trình duyệt, timeout 15s, đọc stream cap 400KB; phần thuần ở monitorLogic (`extractWatchUrl`/`isPrivateHost`/`isWatchableUrl` chống SSRF — chặn localhost/IP nội bộ/metadata, `parseWatchAuth` cookie hoặc dòng "Header: value", `stripHtml` cap 12k chars); `detectSourceType` nhận 'url' khi keyword chứa link (rule cũ tự hưởng). (3) run-monitor `monitorUrl`: flash-lite trích {found, login_required, value, tóm tắt, changed, matches} từ text trang (0 grounding); cổng gửi như provider (condition → chấm trên nội dung thật + cooldown 6h/±3%; đặt giờ luôn gửi; important-mode chỉ gửi khi ĐỔI); 401/403 hoặc login_required → thông báo "🔒 cần đăng nhập" hướng dẫn cấp quyền, dedup không spam, watch_auth có sẵn mà vẫn bị chặn → báo "quyền hết hạn"; lỗi trang → bỏ lượt (KHÔNG đốt grounding); previewGate hỗ trợ url (nhãn 🌐 ở Trang test). (4) generate-rule: source_type "url" + watch_url (thiếu link → hỏi xin; trang cần login → nhắc cấp quyền trong message; app thuần mobile → giải thích không theo dõi được); validate ^https?:// server-side. (5) Client: preview card hiện "Trang web"; insert đính kèm 2 cột chỉ khi là rule url; rule-detail hiện "Trang theo dõi" + khối "Cấp quyền đăng nhập" (dán Cookie, Lưu/Thu hồi, báo thiếu 0018). Deploy run-monitor + generate-rule (smoke 401 OK); web đã export nhưng `eas deploy --prod` + OTA bị chặn quyền — user tự chạy. 11 suite / 113 test pass (thêm 7 test url watch), tsc + eslint sạch.
- 2026-07-03: **FIX nhắc hẹn bắn 2 lần + bắn trước giờ**. User báo 1 rule nhắc-1-lần nhận 2 thông báo liên tiếp. Nguyên nhân: nhiều lượt run-monitor có thể chạy CHỒNG nhau (cron chính 15' + reminder-tick mỗi phút + quét khi mở app + nút "Kiểm tra tin ngay") — 2 lượt cùng thấy "chưa bắn" → cùng push. Sửa 3 lớp trong run-monitor: (1) **CLAIM nguyên tử**: monitorReminder trước khi bắn phải giành quyền bằng UPDATE có điều kiện (`last_run_at < remind_at` → set now) — Postgres bảo đảm chỉ 1 lượt nhận được row, lượt thua thấy 0 row thì im (chặn MỌI tổ hợp race, không riêng cron+tick). (2) Cờ `reminderOnly` cho reminder-tick: tick chỉ xử lý nhắc hẹn, không đụng rule tin tức (đỡ quét chồng + đỡ quota); 0017 cập nhật body `{"reminderOnly": true}`. (3) **Chốt "chưa tới giờ"**: quét thủ công (ruleId) bỏ qua isDue → trước đây bấm "Kiểm tra tin ngay" với nhắc hẹn TƯƠNG LAI là bắn luôn; giờ monitorReminder tự kiểm `now >= remind_at` mới bắn. LƯU Ý key 0017: anon key sẽ bị run-monitor 401 (không phải admin, không phải JWT user) → tick chết im — PHẢI dùng legacy service_role JWT, dán trực tiếp SQL Editor, không commit. tsc sạch, 106/106 test, deploy run-monitor.
- 2026-07-02 (tiếp 2): **Nhắc hẹn bỏ giới hạn 30 phút + chính xác tới phút**. (1) generate-rule: quy tắc "tối thiểu 30 phút" nói rõ CHỈ áp cho định kỳ, KHÔNG áp nhắc hẹn; dạy giờ TƯƠNG ĐỐI ("5 phút nữa"/"2 tiếng nữa"/"tối nay" → cộng từ giờ VN hiện tại đã tiêm vào prompt) + ví dụ I. (2) `dueAt` của reminder = đúng `remind_at` → reminder tới hạn lên ĐẦU hàng đợi quét (nhạy thời gian nhất, 0 AI). (3) Migration `0017_reminder_tick.sql` (CHỜ USER CHẠY, cần thay PROJECT_REF + legacy service_role JWT): cron phụ MỖI PHÚT nhưng SQL-gated — `select net.http_post(...) where exists(nhắc hẹn tới hạn chưa bắn)` → phút nào không có hẹn thì 0 gọi mạng, có hẹn thì bắn ngay trong phút. Chưa chạy 0017 thì nhắc hẹn vẫn hoạt động, trễ tối đa ~15'. Deploy run-monitor + generate-rule; 106 test pass (thêm 2: dueAt reminder + nhắc 5 phút).
- 2026-07-02 (tiếp): **FIX "rule 8h mấy hôm liền không báo" — rule đặt giờ = LỊCH HẸN, không phải bộ lọc**. Nguyên nhân gốc: khung bắn chỉ [giờ hẹn, +15') — lượt cron đúng giờ mà dính 429 quota (sáng quota hay cạn, reset ~14h VN) hoặc bị deadline 70s cắt (nhiều rule cùng hẹn 08:00) → `diff>=15` → MẤT NGUYÊN NGÀY, lặp nhiều hôm. Sửa 4 chỗ: (1) `isDue`: khung catch-up [giờ hẹn, +4h) — lỡ lượt nào thử lại lượt sau tới khi quét thành công; chống bắn lặp bằng so `last_run_at` với MỐC HẸN gần nhất (không phải cửa sổ); guard chu kỳ trừ hao 5h để hôm-qua-bắn-muộn không làm trượt mốc đúng giờ hôm nay. (2) `monitorRule`: rule đặt giờ BỎ QUA chế độ "chỉ tin quan trọng" + fallback ĐƯỢC push (bản tin đúng hẹn là thứ user chủ động yêu cầu — 2 thay đổi chống-rác trước đó vô tình nuốt nó). (3) Banner lọc rác ở màn Rules loại rule có `run_at`. (4) previewGate (Soi bộ lọc) phản ánh đúng. Helper `isScheduled` + `SCHEDULED_CATCHUP_MIN` ở monitorLogic; test isDue viết lại + 3 case mới (83 test pass). Deploy run-monitor + web + OTA.
- 2026-07-02: **6 cải tiến chống rác + độ tin cậy** (một đợt). (1) **SỬA GỐC "không nhận tin thật"**: vòng lặp chết cũ = Gemini chỉ trả 1 bài nổi nhất → trùng tiêu đề đã gửi → chặn mãi → toàn filler. Giờ: truyền ~8 tiêu đề BÀI THẬT đã gửi vào prompt bảo Gemini NÉ + Gemini trả TỐI ĐA 3 bài + server chọn bài đầu tiên CHƯA TRÙNG (`pickFreshItem`). (2) **Freshness**: Gemini trả thêm `published_date`, bỏ bài cũ >7 ngày (không rõ ngày thì giữ). (3) **Dọn push token chết**: đọc ticket Expo, token `DeviceNotRegistered` bị xóa khỏi DB. (4) **Cảnh báo quota**: cron vượt 1.200/1.500 lượt/ngày (mốc reset giờ Pacific) → push cho admin đúng 1 lần/ngày. (5) **Banner màn Rules**: có rule định kỳ-không-điều-kiện còn chế độ Đầy đủ → gợi ý bật "Chỉ báo tin quan trọng" 1 chạm cho cả loạt (đóng banner thì nhớ `@noise_banner_hidden`; thiếu cột 0015 báo rõ). (6) **Tách logic thuần ra `_shared/monitorLogic.ts`** (lịch isDue/dueAt/intervalMs, quiet hours, normTitle/Val, pickFreshItem, isTooOld, recentRealTitles) — run-monitor import lại + **19 unit test mới** (`__tests__/lib/monitorLogic.test.ts`). LƯU Ý bug né được: `.filter(isDue)` sẽ truyền index làm nowMs → phải `.filter((r) => isDue(r))`. previewGate (Soi bộ lọc) dùng chung pickFreshItem nên trang test phản ánh đúng hành vi thật. Deploy run-monitor (smoke 401 OK). 10 suite / 80 test pass.
- 2026-07-01: **Filler KHÔNG đẩy push nữa (mọi rule)**. Người dùng báo vẫn nhận rác "chưa có thay đổi" vì rule cũ vẫn `notify_mode='all'` (mặc định) — tính năng auto chỉ áp lúc TẠO rule mới. Sửa GỐC: `insertNotif` thêm tham số `push=true`; 3 nhánh fallback (nochange/related/none) gọi với `push=false` → filler chỉ lưu trong app, KHÔNG rung máy, không phụ thuộc notify_mode. Push giờ chỉ dành cho TIN THẬT (nhánh 1). Deploy run-monitor. *(Vấn đề #2 "gần như không nhận tin thật" đang chẩn đoán bằng "Soi bộ lọc" ở Trang test — nghi chống-trùng theo tiêu đề quá gắt: Gemini trả cùng 1 bài ổn định → not fresh → rơi vào filler nochange mãi.)*
- 2026-06-30: **Trang test: "Soi bộ lọc rác"**. Thêm nút phễu trên mỗi rule (mục 4 Trang test) chạy 1 lượt Gemini THẬT cho rule đó rồi cho biết CẢ 2 chế độ thông báo sẽ quyết định gì với tin tìm được — KHÔNG ghi notification nào (không làm bẩn DB). run-monitor thêm cờ `gateCheck` + hàm `previewGate` (tính lại các cổng condOk/changeOk/fresh/importantOk như monitorRule nhưng trả quyết định thay vì insert); admin-api action `gate_check`; type `GatePreview` ở lib/admin; UI diễn giải "✅ Vẫn báo / 🚫 Bị lọc — [lý do]". So sánh trực quan chế độ Đầy đủ vs Chỉ tin quan trọng trên dữ liệu thật, tốn 1 quota/lần. Deploy run-monitor + admin-api. tsc/eslint sạch, 61/61 test pass.
- 2026-06-30: **Chống thông báo rác — chế độ "chỉ tin quan trọng" theo từng rule**. Thêm cột `notify_mode` ('all' | 'important', mặc định 'all' — migration `0015_rule_notify_mode.sql`, CHỜ user chạy). Khi `important`: run-monitor (1) BỎ HẲN fallback ("chưa có thay đổi"/"chưa tìm thấy"/gợi ý liên quan), (2) tin thật chỉ qua khi `is_important` HOẶC thỏa điều kiện rule (`importantOk` gate) → cắt cả filler lẫn tin lạc đề. generate-rule giờ chấm thêm `noise_risk` ('low'/'high') + `noise_reason` mỗi rule (chủ đề rộng/định kỳ trên thứ ít biến động = high; có condition hoặc 'change' bị ép về low ở server). create-rule: rule `noise_risk=high` hiện hộp cảnh báo + tick "Chỉ báo tin quan trọng" BẬT SẴN — đồng ý thì lưu `important`, bỏ tick thì `all`. rule-detail: nút funnel chuyển Đầy đủ ⇄ Chỉ tin quan trọng bất cứ lúc nào. Deploy run-monitor + generate-rule. tsc/eslint sạch, 61/61 test pass.
- 2026-06-30: **Kết quả quét dễ đọc trên Trang test**. run-monitor giờ trả thêm `notes[]` (mỗi rule: keyword + title + `kind`): `real` (tin thật) / `nochange` / `related` / `none` (3 loại fallback) / `skipped` (điều kiện chưa thỏa hoặc đã có tb trong chu kỳ). `monitorRule` gán `note` ở từng nhánh insert. Trang test diễn giải thành dòng có nhãn (✅ Tin thật / ➖ Fallback… / ⏭️ Bỏ qua) kèm tiêu đề — khỏi phải mở tab Thông báo để biết rule cho ra cái gì. Type `RunResult`/`RunNote` ở lib/admin. Deploy run-monitor.
- 2026-06-30: **Trang TEST riêng cho admin** (`app/admin-test.tsx`, lối vào ở Settings cạnh Trang quản trị; đăng ký screen ở `_layout`). Tính năng kiểm thử: (1) **Dự đoán thứ tự quét** (dry-run, 0 quota) — hiện rule nào tới hạn + thứ tự sẽ quét theo `dueAt`, rule chưa tới hạn kèm "còn X phút"; kiểm chứng trực tiếp logic ưu tiên vừa sửa. (2) **Chạy cron thử ngay** (quét hết rule tới hạn như cron thật, tốn quota) + hiện kết quả thô. (3) **Chạy thử 1 rule** (tìm kiếm + nút play) → kết quả JSON thô. (4) **Gửi push test cho chính admin**. (5) Pill quota hôm nay. Backend: run-monitor thêm cờ `dryRun` (trả KẾ HOẠCH quét, không gọi Gemini — logic chọn rule nằm DUY NHẤT ở run-monitor, client không tính lại); admin-api thêm action `run_cron` + `scan_preview`. Deploy run-monitor + admin-api. tsc/eslint sạch.
- 2026-06-30: **Sửa thứ tự ưu tiên rule khi quét** (run-monitor). Bỏ sắp xếp SQL theo `last_run_at` tăng dần (sai bản chất: chỉ nhìn "lâu chưa quét", bỏ qua chu kỳ riêng từng rule). Thay bằng sắp `dueRules` theo `dueAt = last_run_at + intervalMs(frequency)` tăng dần — rule đến hạn báo SỚM NHẤT (trễ hẹn nặng nhất so với chu kỳ riêng) được quét trước, quan trọng khi chạm deadline 70s. Rule chưa từng quét (last_run_at=null) → dueAt nhỏ → tự lên đầu. Deploy done.
- 2026-06-30: **Sửa logic quét rule** (run-monitor). (1) Bỏ `MAX_RULES_PER_RUN=2` — thay bằng pre-filter `isDue` rồi quét hết, deadline 70s làm safety net. Trước đây cap cứng làm rule bị bỏ qua oan dù đến hạn. (2) Fallback "chưa có thay đổi" dùng `intervalMs(rule.frequency)` thay vì `vnDayStart` — rule 1 tiếng giờ gửi fallback mỗi tiếng thay vì chỉ 1 lần/ngày. Deploy done.
- 2026-06-30: **Share + Rule filter**. (1) `notification-detail`: nút `share-social-outline` trên header → `Share.share()` gửi tiêu đề + tóm tắt AI (hoặc nội dung) + link bài gốc. (2) `notifications` tab: chips filter theo rule (horizontal ScrollView, chỉ hiện khi >1 rule); bấm chip lọc — kết hợp được với tab Chưa đọc/Quan trọng và search. tsc 0 lỗi.
- 2026-06-24 (deploy): Đẩy toàn bộ thay đổi JS lên cả 2 nền (không build lại APK vì không thêm native dep). (1) **App mobile** — `eas update --branch preview` (channel preview ↔ branch preview, APK đã cài nhận OTA): update group 24e48586, runtime 1.0.0, android+iOS. (2) **Web** — `expo export --platform web` (output "single") → `eas deploy --prod`: live tại https://ai-notifier-new.expo.app (HTTP 200). LƯU Ý: `eas-cli` gọi bằng `npx eas-cli ...` (không phải `npx eas`); `eas deploy` log "export: static" chỉ là static hosting/SPA, KHÔNG phải web.output="static" (vẫn "single"). Edge function `admin-api` VẪN cần deploy riêng qua Dashboard (CLI supabase lỗi uv_spawn) — không gấp.
- 2026-06-24 (tiếp): User đã CHẠY migration 0014. Dọn nốt: (a) bỏ workaround xóa-notifications-thủ-công ở `admin-api` (rule delete + delete_user) và client (`rules.tsx` + `rule-detail.tsx`) — để FK CASCADE tự dọn; (b) khử hết warning eslint: 7 chỗ `exhaustive-deps` khai báo rõ chủ đích bằng `eslint-disable-next-line` + lý do (fetch chỉ đọc user/id đã có trong deps), thêm override cho `__tests__/**` cho phép `require()` trong jest mock, gỡ `mockBack` thừa. **Trạng thái: eslint 0 vấn đề, tsc 0 lỗi, 61/61 test pass.** CẦN DEPLOY lại edge function `admin-api` (không deploy được từ máy dev — `uv_spawn`; chạy `npx supabase functions deploy admin-api`). Không gấp: bản cũ vẫn đúng, chỉ thừa 1 lệnh delete vô hại.
- 2026-06-24: Rà soát toàn app + sửa lỗi/nợ kỹ thuật. (1) Lỗi ESLint rules-of-hooks: `useTemplate` (hàm thường, tên `use*`) → đổi `applyTemplate`. (2) Bộ test SỐNG LẠI: trước 8/9 suite crash vì thiếu mock — thêm vào `__tests__/setup.ts` mock `@react-native-async-storage/async-storage` (jest mock chính thức), `expo-font.isLoaded/isLoading` (@expo/vector-icons cần), và `react-native-safe-area-context` (mock INLINE vì file `.tsx` của lib không nằm trong transformIgnorePatterns whitelist). Cập nhật các test lệch theo UI mới: login/register (placeholder Việt + register thiết kế lại 5 trường + signUp kèm metadata), notification-detail ("AI tóm tắt"), home ("N tin quan trọng"), notifications (mock `useFocusEffect`, tab vs badge trùng text dùng getAllByText[0]), rule-detail/rules (Switch không có onValueChange → test `fireEvent.press` để bubble lên TouchableOpacity cha), create-rule (viết lại: mock `lib/ruleAI.chatRule` thay timer giả; thêm `testID="send-button"`). KẾT QUẢ: **9/9 suite, 61/61 test PASS** (trước chỉ 5 test chạy). (3) ESLint bỏ lint `supabase/functions/**` (Deno/esm.sh) → 0 error (trước 3). (4) Dọn: gỡ `EXPO_PUBLIC_ANTHROPIC_KEY` thừa khỏi `.env`; đổi tên `lib/claude.ts` → `lib/ruleAI.ts` (thực chất gọi Gemini, không phải Claude); sửa import-giữa-file ở admin.tsx. (5) Migration `0014_notifications_fk_cascade.sql`: đặt `ON DELETE CASCADE` cho `notifications.rule_id` (DO block tự tìm & thay FK cũ) → bỏ được workaround xóa-con-thủ-công, user thường xóa rule không còn dính FK 23503. **CẦN CHẠY 0014 trên Supabase SQL Editor.** Đã xác minh `google-services.json` chỉ là config CLIENT Firebase (api_key công khai) → đúng để commit, GIỮ nguyên. Còn lại 11 warning eslint đều là `exhaustive-deps` cố ý (throttle fetch).
- 2026-06-20: Giảm tải quét free tier — `MAX_RULES_PER_RUN` 8 → 4 (cron quét ít rule/nhịp hơn để đỡ chạm quota Gemini free). Deploy run-monitor. Bối cảnh: thử bật Gemini trả phí (Tier 1) để hết quota nhưng thẻ (Visa ảo MB) bị Google từ chối `OR_BACR2_31` ở khâu lập billing recurring (thẻ trả Claude 1 lần thì OK). TẠM giữ free tier; sẽ thử lại sau (Buy credits 1 lần / đợi 24h / thẻ credit/recurring). Build APK mới gộp toàn bộ thay đổi: build d9243f56.
- 2026-06-20: Thêm NHÃN RULE trên mỗi thông báo (tab Thông báo) — chip có tên rule (📌) ở đầu card để nhìn là biết thông báo của rule nào. fetchNotifications lấy thêm title/keyword → map rule_id→tên. Sửa JS, test Expo Go/web ngay.
- 2026-06-20: 429 quota Gemini — grounding (Google Search) free tier bị siết; run-monitor luôn bật grounding nên cron toàn "quét 0 ⚠️" (call không grounding như generate-rule vẫn chạy). Lỗi rõ: "You exceeded your current quota...". TẠM giãn nhịp (chờ reset ~14-15h VN): MAX_RULES_PER_RUN 4→2, "theo điều kiện" 15→60 phút, thêm sleep 4s giữa các call né rate-limit theo phút. Trang admin: hiện "Lỗi gần nhất" (bấm xem full), bắt lỗi 300→800 ký tự. HƯỚNG BỀN sau: đổi sang RSS + Gemini tóm tắt (né grounding) hoặc bật billing.
- 2026-06-20: Deploy web PUBLIC lên EAS Hosting. Lệnh: `npx expo export -p web` → `eas deploy --prod`. **Production URL: https://ai-notifier-new.expo.app** (ai mở trên PC/điện thoại cũng được, không cần cài). Biến `.env` (EXPO_PUBLIC_*) tự nhúng khi export; ANTHROPIC_KEY rỗng nên không lộ. Đẩy bản web mới: export lại + `eas deploy --prod`; client tự nạp lại nhờ webAutoUpdate. (Lưu ý lỗi "bucket does not exist" lần đầu — chạy lại lệnh là được.)
- 2026-06-20: Android/iOS tự cập nhật OTA bằng EAS Update — cài `expo-updates`, `eas update:configure` (app.json: `updates.url` + `runtimeVersion.policy=appVersion`). `lib/nativeAutoUpdate.ts` gọi từ `_layout.tsx`: khi mở app `checkForUpdateAsync`→`fetchUpdateAsync`→`reloadAsync` (no-op trên web/Expo Go/dev). CẦN 1 LẦN: build APK mới có expo-updates rồi cài (`eas build -p android --profile preview`). Sau đó đẩy bản JS bằng `eas update --branch <branch>` là máy tự nhận, không gỡ-cài lại.
- 2026-06-20: Web tự cập nhật bản mới (`lib/webAutoUpdate.ts`, gọi từ `app/_layout.tsx`, chỉ web). Định kỳ 60s + khi quay lại tab: fetch `/?v=<ts>` no-store → trích chữ ký file js/css băm trong index.html, khác bản đang chạy = vừa deploy → `location.reload()`. Chặn reload khi đang gõ (input/textarea/contentEditable) để khỏi mất nội dung. Hết cảnh xoá cache + tải lại tay.
- 2026-06-20: Cải thiện nội dung thông báo (prompt run-monitor, áp dụng MỌI thông báo): (1) thay đổi số liệu phải nêu RÕ "từ [mức cũ] → [mức mới]" thay vì chỉ chênh lệch; (2) ưu tiên đơn vị/tiền tệ Việt Nam, số liệu nước ngoài kèm quy đổi trong ngoặc (trừ khi rule yêu cầu đơn vị nước ngoài). Sửa SEARCH_SYSTEM + field content/ai_summary. Deploy lại run-monitor.
- 2026-06-20: FIX auto không gửi — cron bị 401 "UNAUTHORIZED_INVALID_JWT_FORMAT". Nguyên nhân: project dùng API key kiểu MỚI (`sb_publishable_`/`sb_secret_`, KHÔNG phải JWT), nhưng Edge Function (verify_jwt) yêu cầu JWT hợp lệ ở header. Cron đang gắn `sb_secret_` → bị từ chối. Sửa: lấy **legacy service_role JWT** (`eyJ...`) ở Settings → API Keys → Legacy API keys, chạy lại `cron.schedule` migration 0004 với key đó. Sau sửa: `net._http_response` trả 200 `{"inserted":2,"checked":2}` → cron chạy thật. Lưu ý: chẩn đoán bằng `select status_code, content from net._http_response order by created desc` (cron.job_run_details "succeeded" chỉ là đã GỬI request, không phản ánh kết quả thật). Còn thấy `quotaHit:true` do backlog rule dồn lại sau nhiều giờ 401 → tự hồi dần (giữ last_run_at khi quota).
- 2026-06-20: FIX link "Đọc bài gốc" trỏ sai bài. Trước đây dùng `top.source_url` (AI tự ghi → hay bịa URL sai). Đổi ưu tiên URL grounding THẬT (Google Search metadata). Vì URL grounding đổi mỗi lần gọi nên đổi chống-trùng sang THEO TIÊU ĐỀ (seenTitles) thay vì theo URL. Deploy lại run-monitor.
- 2026-06-20: FIX bàn phím che ô nhập (create-rule + manual-rule). Do edge-to-edge Android không tự resize, `KeyboardAvoidingView behavior` để `undefined` trên Android → bàn phím che chat. Đổi `behavior="padding"` cho cả 2 nền. (register dùng "height" nên không sao.) Sửa JS — test ngay trong Expo Go.
- 2026-06-20: Thương hiệu "Nofy". Đổi tên app hiển thị `ai-notifier-new` → **"Nofy"** (app.json name; slug/package giữ nguyên). Icon mới: cắt từ logo Nofy gốc (`assets/images/nofy-soure.png`) bằng Pillow (scripts/crop_icon.py), chọn tông VIBRANT (hồng-tím→xanh, rực rỡ trẻ trung) (scripts/finalize_icon.py) → `icon.png` + `adaptive-icon.png` (backgroundColor #FEFEFE, bỏ background/monochrome cũ). CHƯA BUILD — gộp với các thay đổi sau rồi build 1 thể. Scripts ở scripts/ (make_icon=vẽ tay đã bỏ, crop_icon, variants, finalize_icon).
- 2026-06-20: Push Android (FCM) hoàn tất phía hạ tầng. Tạo Firebase project `ai-notifier-52174`, thêm app Android `com.hiep1607.ainotifiernew`, gắn `google-services.json` vào app.json (`android.googleServicesFile`) + commit; `fcm-service-account.json` gitignore (bí mật). Nạp FCM V1 service account key lên Expo credentials (web). Build APK mới có FCM: build d04c3cb8 → FINISHED. CÒN: user cài APK mới + bật Autostart/bỏ giới hạn pin (Xiaomi/MIUI) + cấp quyền thông báo → test push thật.
- 2026-06-20: Build Web + Android. Web: `npx expo export --platform web` → `dist/` OK. Android: thêm `android.package`/`ios.bundleIdentifier`/`owner: hiep1607` = `com.hiep1607.ainotifiernew`, tạo `eas.json` (profile `preview` = APK). Đã đăng nhập EAS (hiep1607), `eas init` tạo project @hiep1607/ai-notifier-new (projectId 6f7e9eb6... ghi vào app.json), keystore tự tạo. QUAN TRỌNG: build cloud KHÔNG có `.env` → đã nạp `EXPO_PUBLIC_SUPABASE_URL` + `ANON_KEY` vào EAS env (preview+production, plaintext) qua `eas env:create`, build lại OK. Build APK đang chạy: build 7d5f6db1. CÒN LÀM: push Android cần FCM credentials (Firebase) — làm sau khi cài APK.
- 2026-06-20: Tối ưu chi phí AI — tách model theo task: `geminiGenerate` thêm tham số `model?` (mặc định giữ `gemini-2.5-flash`); generate-rule (chỉ trích JSON, không cần grounding) hạ xuống `gemini-2.5-flash-lite` ($0.10/$0.40, rẻ ~33%, không giảm chất lượng); run-monitor GIỮ `gemini-2.5-flash` để còn Google Search grounding (free 1.500 lượt/ngày). Deploy lại generate-rule. (Kết luận khảo sát: ở quy mô cá nhân grounding đang miễn phí nên giữ kiến trúc Gemini; đổi sang API rẻ hơn không tiết kiệm đáng kể mà mất grounding → tin dễ bịa.)
- 2026-06-20: Giờ yên lặng (tùy chỉnh) — Settings có toggle + 2 slider chọn giờ; server `run-monitor` bỏ qua push trong khung (vắt nửa đêm OK), tin vẫn vào app. Lưu DB `user_settings`. LƯU Ý: bảng `user_settings` cũ trùng tên đã tồn tại sẵn với FK sai (trỏ sai bảng) gây loạt lỗi PGRST204 → 42P10 → 23503; đã DROP + tạo lại sạch (`user_id` PK → auth.users). Client dùng update→insert (không upsert onConflict) cho an toàn ràng buộc. Verify lưu OK.
- 2026-06-20: "Để êm" theo rule (mute push, cột `muted`, migration 0010); nhóm thông báo theo ngày; empty state có nút CTA; AI Insight động (tin thật); onboarding 3 slide lần đầu; badge tab cap "99+".
- 2026-06-19: Nút "+" tạo rule (màn Rules) giờ KÉO THẢ tự do được (Gesture.Pan + reanimated), vẫn bấm để mở menu (Gesture.Tap, Race); nhớ vị trí qua AsyncStorage @fab_pos. Chạy cả web lẫn native.
- 2026-06-19: FIX mobile — (1) cuộn không tới cuối ở màn chi tiết (nút dưới bị thanh điều hướng Android che): thêm SafeAreaProvider + paddingBottom theo insets.bottom cho notification-detail & rule-detail. (2) Công tắc bật/tắt rule giờ BẤM được (bọc TouchableOpacity + Switch pointerEvents none + hitSlop) ở rule-detail & danh sách Rules.
- 2026-06-19: FIX crash/màn trắng trên Expo Go (native) — Reanimated 4 nhưng babel còn dùng plugin cũ `react-native-reanimated/plugin` → worklet không biên dịch → crash native (web vẫn chạy). Đổi sang `react-native-worklets/plugin`. Bọc app bằng `GestureHandlerRootView` (cần cho vuốt-xóa trên native). CẦN restart `npx expo start -c` (xóa cache) để babel ăn.
- 2026-06-19: Chi tiết rule — đưa 2 nút "Kiểm tra tin ngay" + "Xóa rule" lên TRÊN danh sách thông báo; mặc định chỉ hiện 5 thông báo gần nhất, thêm nút "Xem tất cả thông báo (N)" / "Thu gọn" để xổ hết (fetch tối đa 50). Chỉ sửa client.
- 2026-06-19: Polish UI/UX — Rules: kích hoạt thanh tìm kiếm ở header (trước là icon chết), thẻ rule hiện lịch (formatSchedule) + điều kiện, empty state theo tab/tìm kiếm. Home: thêm dòng lịch + điều kiện vào thẻ rule đang hoạt động. Chỉ sửa client, tsc sạch.
- 2026-06-19: Fallback "chưa có thay đổi" giờ link về THÔNG BÁO TRƯỚC trong app (không có URL bài mới) — thêm cột `related_notification_id` (migration 0009), run-monitor lưu id tb gần nhất, notification-detail hiện nút "Xem thông báo trước" khi source_url rỗng. Deploy lại run-monitor.
- 2026-06-19: Phân quyền run-monitor — chống lạm dụng anon key (công khai). Phân biệt cron (service_role = admin, quét tất cả) với người dùng (JWT đăng nhập): xác thực token, lấy userId từ token thay vì body, chỉ quét rule của mình; chặn ruleId người khác (403) và anon thuần (401). Test 401 OK. Deploy lại run-monitor.
- 2026-06-19: Luôn-gửi cho rule định kỳ/đặt giờ — không tìm thấy tin vẫn gửi tb: có tb trước → "chưa thay đổi" + link trỏ tb trước; chưa có → "chưa tìm thấy" + tin liên quan gần nhất; tuyệt đối không im. Rule "theo điều kiện" giữ im khi chưa thỏa. Rule đặt giờ gửi đúng giờ [target, target+15). Deploy lại run-monitor.
- 2026-06-19: Bỏ phụ thuộc cột last_value cho #8 — khi chưa có mốc cũ thì KHÔNG tự nhận "đã đổi" (hết báo lặp mỗi phiên); cơ chế chính = AI chấm điều kiện rule mỗi phiên + dedup URL. Deploy lại run-monitor.
- 2026-06-19: Quét sớm cho rule ĐẶT GIỜ — khung [target-15, target+15): bắn sát giờ kể cả cron lỡ nhịp; xử lý mốc gần nửa đêm (vòng 24h). KHÔNG áp cho định kỳ thuần (tránh trôi tần suất). Deploy lại run-monitor.
- 2026-06-19: Soát hệ thống — sửa lỗi "bỏ đói" rule khi >8 rule (cron giờ sắp theo last_run_at tăng dần, xoay vòng công bằng, rule đặt giờ không bị chen). Deploy lại run-monitor.
- 2026-06-19: Sửa chống-trùng cho chủ đề kiểu giá-trị (thời tiết/giá): dedup theo URL + last_value, cùng URL mà số liệu đổi vẫn báo (trước đây URL đứng yên bị chặn vĩnh viễn). + retry generate-rule. Deploy lại run-monitor.
- 2026-06-19: Tách nhiều rule từ 1 câu — generate-rule trả mảng rules, UI nhiều card + tạo hàng loạt. Deploy lại generate-rule.
- 2026-06-19: Trigger "thay đổi" — lưu last_value, so sánh với lần trước để chỉ báo khi số liệu thực sự đổi (migration 0008). Deploy lại run-monitor.
- 2026-06-19: Tối ưu quota Gemini — bỏ quét trùng ở Home (cron là lịch duy nhất), client retry lỗi tạm, server dừng sớm khi 429 + trần 8 rule/lần. Deploy lại run-monitor.
- 2026-06-19: Tạo file kế hoạch. Hoàn tất push notification (code) + deploy 2 function.
- 2026-06-19: Đặt giờ cụ thể (run_at) + sắp xếp rule theo created_at.
- 2026-06-19: Tần suất linh hoạt (số phút, min 30) + ẩn 15 phút cho "Theo điều kiện".
- 2026-06-19: Tạo rule chặt chẽ (hỏi lại / giải thích yêu cầu vô lý).
- 2026-06-19: Thông báo chi tiết + 1/lần + lịch per-rule (last_run_at, cron 15 phút).
- 2026-06-19: Chuyển AI sang Gemini server-side + pg_cron 24/7 (bỏ Ollama).
